#include <stdio.h>

/**
 * main - prints alphabet in lowercase
 * followed by a new line
 *
 * Return: Always 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d\n", n);
	return (0);
}
